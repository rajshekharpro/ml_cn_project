# Core Python dependencies for this workspace on Python 3.10.
#
# Recommended HPC flow:
# 1. Create/activate a Python 3.10 environment.
# 2. If your cluster provides a CUDA-tuned PyTorch build, install that first.
# 3. Then run: `pip install -r requirements.txt`
#
# Non-pip system tools still required for the full ET-BERT data pipeline:
# - mono
# - editcap (Wireshark)
# - SplitCap.exe placed at ET-BERT/SplitCap.exe
#
# Optional packages not listed as hard requirements:
# - dadapy: enables intrinsic dimensionality metrics in run_evaluation.py
# - apex: only needed if you train ET-BERT with --fp16
# - jupyterlab: only needed for notebook-heavy workflows

# Shared numeric / ML stack
torch>=2.1,<3.0
numpy>=1.24,<2.3
scipy>=1.10,<1.16
pandas>=1.5,<2.4
scikit-learn>=1.2,<1.7
tqdm>=4.64,<5.0

# ET-BERT data processing / preprocessing
scapy>=2.5,<2.7
flowcontainer>=7.4
packaging>=23.0
six>=1.16.0
xlrd>=2.0.1
sentencepiece>=0.1.99
tokenizers>=0.15,<0.21

# Evaluation / plotting
matplotlib>=3.7,<3.11
seaborn>=0.12,<0.14

# Additional research-model dependencies used by scripts in demystifying-networks-main/src
transformers>=4.37,<4.53
datasets>=2.16,<4.0
torchvision>=0.16,<0.22
timm>=0.9,<1.1
jieba>=0.42.1
